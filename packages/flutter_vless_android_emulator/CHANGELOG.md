## 1.1.5

* Updated bundled Android emulator `libxray.so` binaries to Xray-core `v26.7.11`.
* Kept the package marked as legacy because current `flutter_vless_android` releases include x86 and x86_64 emulator support in the main Maven runtime AAR.

## 1.1.4

* Updated bundled Android emulator `libxray.so` binaries to Xray-core `v26.6.27`.
* Kept the package marked as legacy because current `flutter_vless_android` releases include x86 and x86_64 emulator support in the main Maven runtime AAR.

## 1.1.3

* Updated bundled Android emulator `libxray.so` binaries to Xray-core `v26.6.22`.
* Kept the package marked as legacy because current `flutter_vless_android` releases include x86 and x86_64 emulator support in the main Maven runtime AAR.

## 1.1.2

* Marked the package as legacy because current `flutter_vless_android` releases include x86 and x86_64 emulator support in the main Maven runtime AAR.

## 1.1.1

* Updated bundled Android emulator `libxray.so` binaries to Xray-core `v26.6.1`.
* Kept x86 and x86_64 emulator support aligned with `flutter_vless_android` 1.1.1.

## 1.1.0

* Aligned the Android emulator package version with the `flutter_vless` 1.1.0 release train.
* Kept x86_64/x86 Android emulator binary support for the main plugin.

## 1.0.2

* XRay version up

## 1.0.1

* XRay version up

## 1.0.0

* initial release
